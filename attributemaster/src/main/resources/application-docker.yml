# Server configuration
server:
  port: 8010
  forward-headers-strategy: framework
  shutdown: graceful

clients:
  dynamic:
    url: http://dynamic-service:9014

# Spring configuration
spring:
  lifecycle:
    timeout-per-shutdown-phase: 1m # The server will wait up to one minute for active requests to complete
  application:
    name: attribute-service # The server application name

  # Db Configuration
  datasource:
    username: postgres
    url: jdbc:postgresql://postgres:5432/attributemaster
    password: imu123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          "[batch_size]": 1000
        "[order_inserts]": true
        # generate_statistics: true
        "[format_sql]": true
    show-sql: true

  output:
    ansi:
      enabled: always

# Eureka Server configuration
eureka:
  instance:
    hostname: attribute-service
    prefer-ip-address: false
  client:
    register-with-eureka: true
    fetch-registry: true
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

# Actuator endpoints configuration
management:
  endpoints:
    web:
      base-path: /rest/actuator
      exposure:
        include: "*"

  # Zipkin configuration
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0

# OpenApi Swagger configuration
springdoc:
  service:
    title: Attribute-Service doc
    version: 1.0.1
  api-docs:
    groups:
      enabled: true
  swagger-ui:
    display-request-duration: true
    operationsSorter: method
    tags-sorter: method
    try-it-out-enabled: true
    deep-linking: true
    defaultModelsExpandDepth: 1
    defaultModelExpandDepth: 1
    display-operation-id: false
    filter: false
    show-extensions: false
  cache:
    disabled: false
  show-actuator: false
  writer-with-default-pretty-printer: true
#logging:
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] ${LOG_LEVEL_PATTERN:-%5p} %m%n"
